# Temporary Scripts

This directory holds experimental or workflow-specific scripts that should not
clutter the top-level `scripts/` directory.

Current contents include:

- VLM-related HyperGym experiments
- VLM -> BC -> RL pipeline entrypoints
- one-off rendering or testing scripts
